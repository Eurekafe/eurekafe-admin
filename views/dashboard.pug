extends ../templates/layout.pug

block body
	h1.text-center dashboard
	div.container
		div.row
			div.col.bg-light
				h2 Newsletter
				p Nombre d'inscrit:  #{count}
				p Dernier inscrit:  #{email}
				button.btn.btn-primary.mx-auto(onclick="window.location = '/newsletter'") Plus
					